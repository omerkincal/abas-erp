package org.omarkincal.abaserpinterview.questionone;

public class QuestionOne {


    // Tüm siparişlerin toplam tutarını hesaplamak için bir metot
    /*public static double toplamTutarHesapla(Map<Integer, Order> orders) {
        double totalAmount = 0;

        for (Order order : orders.values()) {
            for (Product product : order.) {
                totalAmount += product.quantity * product.unitPrice;
            }
        }

        return totalAmount;
    }*/
}
